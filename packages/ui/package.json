{
  "name": "@superbee/ui",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "React SPA for `superbee ui` (plans/ui-v1.md rev 3.2): bundle Views over the wire-protocol v0 surface, served same-origin by the private ui-server workspace runtime. PRIVATE — never enters the CLI's npm dependency graph; only this package's BUILT dist/ assets are gzip-embedded into the CLI's esbuild bundle (packages/cli/scripts/embed-ui-assets.mjs).",
  "license": "MIT",
  "scripts": {
    "prebuild": "npm run build -w @superbee/markdown-renderer",
    "build": "vite build",
    "pretypecheck": "npm run build -w @superbee/markdown-renderer",
    "typecheck": "tsc --noEmit",
    "pretest": "npm run build -w @superbee/markdown-renderer",
    "test": "vitest run",
    "e2e": "playwright test",
    "e2e:gate": "playwright install chromium && playwright test e2e/pages.spec.ts e2e/security.spec.ts e2e/personal-task-system.spec.ts e2e/engineering-review.spec.ts --project=chromium"
  },
  "dependencies": {
    "@superbee/markdown-renderer": "*",
    "@superbee/view-runtime": "*",
    "@tanstack/react-query": "^5.62.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@superbee/core": "*",
    "@superbee/server": "*",
    "@superbee/ui-server": "*",
    "@playwright/test": "^1.49.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.3.4",
    "jsdom": "^25.0.1",
    "typescript": "^5.4.0",
    "vite": "^6.0.7",
    "vitest": "^3.0.0"
  }
}
