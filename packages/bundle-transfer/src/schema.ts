/** Frozen JSON Schema URI for the physical transfer artifact manifest. */
export { BUNDLE_TRANSFER_MANIFEST_V1 } from "./types.js";

/** The schema is shipped as `schema/bundle-transfer-manifest-v1.schema.json`. */
export const BUNDLE_TRANSFER_MANIFEST_SCHEMA_PATH =
  "schema/bundle-transfer-manifest-v1.schema.json" as const;
