\* expect: Action property NoRegress is violated
\* Mutant: the pull fence's per-write guard does not pin the pull marker, so an older pull overwrites a newer one's refresh.
SPECIFICATION Spec
CONSTANTS
  x = x  y = y  v0 = v0  v1 = v1  v2 = v2  t1 = t1  t2 = t2
  Absent = Absent  NoBase = NoBase  None = None
  Threads = {t1, t2}
  Docs = {x, y}
  Vals = {v0, v1}
  InitDoc <- MCInit
  MaxTP = 1
  MaxCommits = 1
  Serialize = FALSE
  PullConsistency = TRUE
  AckInvalidates = TRUE
  PullFence = TRUE
  FenceIgnoresMarker = TRUE
PROPERTY NoRegress
