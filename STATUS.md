# STATUS — frozen 2026-07-06

Per-unit records live on the PROJECT BUNDLE (the deployed board): task descriptions
(`tasks/<unit>`) carry each unit's record, `roadmaps/superbee-platform` carries the forward
sequence, and plans/research are bundle docs (`plans/*`, `research/*`). See `CLAUDE.md` for the
entrypoint and bundle document `docs/core` for the product statement.

The full wired/partial/deferred history through item 47 (2026-07-06) remains intact in this
file's Git history.
